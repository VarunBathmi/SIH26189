# For local dev, leave unset — Vite's dev proxy forwards /api to localhost:5000.
# For production, set this to your deployed backend's URL + /api, e.g.:
# VITE_API_URL=https://forensic-network-analyzer-api.onrender.com/api
VITE_API_URL=
